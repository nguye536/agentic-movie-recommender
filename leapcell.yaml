runtime: python
startCommand: uvicorn main:app --port 8080
buildCommand: pip install -r requirements.txt
port: 8080
memory: 512mb
envs:
  - name: OPENAI_API_KEY
    # Set this secret in the Leapcell dashboard — never commit the actual key
